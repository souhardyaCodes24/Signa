# Signa environment variables
# Copy this file to .env and fill in the values.
# For local development, SQLite is used by default — no env vars needed.

# Production PostgreSQL (Neon) connection string:
# SIGN_DB_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/signa?sslmode=require
